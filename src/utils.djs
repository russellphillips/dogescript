String.format is function() {
    very s is arguments[0]
    much very count as 0 next count smaller (arguments.length - 1) next count more 1
        very reg is new RegExp with "\\{"+i+"\\}" "gm"
        s is plz s.replace with reg (arguments[count]+1)
    wow

wow s